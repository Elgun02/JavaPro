public class Rules {
    // Правило Де Моргана говорит о том что отрицаний двух логического И равно логическому ИЛИ отрицаний каждого высказывание,
    // так же отрицаний логическому ИЛИ двух высказывание равно И отрицаний

    // Пул строк - это механизм для оптимизации использование строк в Java, который позволяет переиспользовать строки
    // и повысить производительность
}
